export const siteConfig = {
  title: 'Frontend Technology Assessment',
  description: `Build a simple app to hit the NY Times Most Popular Articles API and show a list of articles that shows details when items on the list are tapped (a typical master/detail web app). You should use react`,
  url: process.env.NEXT_PUBLIC_API_URL,
  appKey: process.env.NEXT_PUBLIC_API_KEY,
};
